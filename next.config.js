/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable static file serving from public directory
  // Assets will be served from /assets, /dist, etc.
}

module.exports = nextConfig
